#!/usr/bin/env zx

let name = "foo bar";
// await $`mkdir ${name}`;
// await $`cat a.js`.pipe("Readable")
// await $`cat ../../package.json  | grep name`
// await $`c -n header1`
await `ls`
await `pwd`
